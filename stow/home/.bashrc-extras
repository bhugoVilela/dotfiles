# DO NOT DELETE
# this is part of the .dotfiles git repo
# it adds extra configuration to the .zshrc file

# adds ~/.local/bin to PATH
export PATH="$PATH:$HOME/.local/bin"

# configures NVM
export NVM_DIR="$HOME/.nvm"
source $(brew --prefix nvm)/nvm.sh
